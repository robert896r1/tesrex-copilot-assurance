# Required safety assertion for tenant-connected commands.
# Export this value before running a live probe. The tool will stop unless it
# exactly matches the tenant ID returned by `az account show`.
AZURE_TENANT_ID=

# Optional Azure CLI setting. Azure CLI reads this environment variable
# directly; the starter kit does not parse .env files automatically.
AZURE_CONFIG_DIR=.azcfg
